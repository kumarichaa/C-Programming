//Write a c++ program to calculate of square of a number.

#include<stdio.h>
#include<conio.h>
main()
{
    int x,y;
    printf("Enter a number");
    scanf("%d",&x);
    y=x*x;
    printf("Square is %d is %d",x,y);
    getch();
}
